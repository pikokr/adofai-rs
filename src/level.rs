pub mod level {
    pub struct Level {
    }

    impl Level {
        pub(crate) fn load(file_content: &str) {
            panic!("Not implemented");
            // let i = serde_json::from_str(file_content);
            // println!("{:?}", i);
        }
    }
}